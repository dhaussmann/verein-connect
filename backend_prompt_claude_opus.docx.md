

**CLAUDE OPUS 4.6 – BACKEND PROMPT**

Vereinsverwaltung SaaS – Cloudflare Workers Backend

 

Hono.js • Drizzle ORM • D1 • R2 • Durable Objects • KV

Vollständiger Prompt für die Backend-Implementierung

*Version 1.0 – März 2026*

# **Hinweise zur Verwendung**

Dieses Dokument enthält den vollständigen Prompt für eine neue Claude Opus 4.6 Instanz, die das Backend für die Vereinsverwaltung-SaaS-Plattform implementiert. Der Prompt ist in **Phasen** aufgeteilt:

| Phase | Inhalt | Prio |
| :---- | :---- | :---- |
| Phase 1 | Projekt-Setup: Monorepo, Hono.js Worker, Drizzle ORM, D1-Schema, wrangler.toml | MUSS |
| Phase 2 | Auth-System: Register, Login, JWT (Ed25519), Refresh-Token, Middleware | MUSS |
| Phase 3 | CRUD-APIs: Mitglieder, Rollen, dynamische Profilfelder, Familien  | MUSS |
| Phase 4 | Events & Kurse: CRUD, Anmeldung, Warteliste, Anwesenheit | MUSS |
| Phase 5 | Kommunikation: Nachrichten, E-Mail-Versand, Chat (Durable Objects) | SOLL |
| Phase 6 | Finanzen: Rechnungen, PDF-Generierung, Stripe, Buchhaltung | SOLL |
| Phase 7 | Extras: Webshop, Materialbank (R2), DSGVO, Audit-Log | KANN |

**Empfohlener Workflow:** Kopiere den gesamten Prompt (ab Seite 3\) in eine neue Claude-Konversation. Claude wird Phase für Phase abarbeiten. Nach jeder Phase Review und Tests durchführen, bevor du die nächste Phase freigibst.

*Wichtig: Der Prompt enthält das Repository https://github.com/dhaussmann/verein-connect als Referenz. Claude soll den Code analysieren und das Backend exakt auf die Frontend-Interfaces abstimmen.*

# **Claude Opus 4.6 – Backend Prompt**

*\--- BEGINN PROMPT (alles ab hier in Claude einfügen) \---*

| Handle als Senior Full-Stack-Entwickler und Cloudflare-Architekt. Du baust das komplette Backend für eine Vereinsverwaltungs-SaaS-Plattform. \== KONTEXT \== Das Frontend existiert bereits und wurde mit Lovable erstellt:   Repository: https://github.com/dhaussmann/verein-connect   Tech: React 18 \+ TypeScript \+ Tailwind CSS \+ shadcn/ui \+ React Router v6   State: TanStack Query \+ Zustand   Sprache: Deutsch (alle UI-Texte, Datumsformat DD.MM.YYYY, EUR) Deine Aufgabe: Baue das Backend als Cloudflare Workers API, die exakt zu den Frontend-Interfaces und API-Calls passt. WICHTIG: Analysiere ZUERST das Frontend-Repository, insbesondere:   1\. src/types/ – TypeScript Interfaces (User, Event, Invoice etc.)   2\. src/services/ oder src/lib/api.ts – API-Calls und Endpunkte   3\. src/data/mockData.ts – Mock-Daten-Strukturen   4\. src/hooks/ – React Query Hooks (zeigen erwartete API-Signaturen)   5\. src/pages/ – Welche Daten jede Seite benötigt Leite aus dem Frontend-Code die exakten API-Contracts ab:   \- Welche Endpunkte werden aufgerufen?   \- Welche Request/Response-Shapes werden erwartet?   \- Welche Query-Parameter und Filter gibt es?   \- Welche Paginierung wird verwendet? Falls das Frontend noch Mock-Daten nutzt statt echter API-Calls, leite die API-Contracts aus den TypeScript-Interfaces und der Mock-Daten-Struktur ab. |
| :---- |

## **Phase 1: Projekt-Setup & Datenbank**

| \== TECH-STACK (VERPFLICHTEND) \== Runtime:       Cloudflare Workers (ES Modules Syntax) Framework:     Hono.js v4+ (Web-Framework für Workers) ORM:           Drizzle ORM (D1-Adapter, drizzle-orm \+ drizzle-kit) Datenbank:     Cloudflare D1 (SQLite am Edge) Datei-Storage: Cloudflare R2 (S3-kompatibel) Cache/Session: Cloudflare KV Echtzeit:      Cloudflare Durable Objects \+ WebSockets Sprache:       TypeScript (strict mode) Validierung:   Zod (Request/Response-Schemas) Auth:          JWT mit Ed25519 (jose Library) Passwort:      bcryptjs (bcrypt-Hashing im Worker) UUID:          crypto.randomUUID() (native in Workers) \== PROJEKT-STRUKTUR \== Erstelle folgende Monorepo-Struktur (das Frontend existiert bereits im Root als React-App, das Backend wird als separates Package angelegt): verein-connect/ ├── src/                    \# Frontend (bereits vorhanden, NICHT ÄNDERN) ├── api/                    \# ← NEUES Backend-Package │   ├── src/ │   │   ├── index.ts          \# Hono App Entry Point │   │   ├── db/ │   │   │   ├── schema.ts       \# Drizzle Schema (alle Tabellen) │   │   │   ├── migrations/     \# D1 SQL Migrationen │   │   │   └── seed.ts         \# Seed-Daten (Rollen, Demo-Verein) │   │   ├── routes/ │   │   │   ├── auth.ts         \# /auth/\* │   │   │   ├── members.ts      \# /v1/members/\* │   │   │   ├── roles.ts        \# /v1/roles/\* │   │   │   ├── events.ts       \# /v1/events/\* │   │   │   ├── courses.ts      \# /v1/courses/\* │   │   │   ├── attendance.ts   \# /v1/attendance/\* │   │   │   ├── communication.ts \# /v1/messages/\*, /v1/chat/\* │   │   │   ├── finance.ts      \# /v1/invoices/\*, /v1/accounting/\* │   │   │   ├── shop.ts         \# /v1/shop/\* │   │   │   ├── files.ts        \# /v1/files/\* │   │   │   └── settings.ts     \# /v1/settings/\* │   │   ├── middleware/ │   │   │   ├── auth.ts         \# JWT-Validierung \+ User-Context │   │   │   ├── rbac.ts         \# Role-Based Access Control │   │   │   ├── org-context.ts  \# Multi-Tenancy org\_id Injection │   │   │   ├── rate-limit.ts   \# Rate Limiting │   │   │   └── validation.ts   \# Zod-Schema-Validierung │   │   ├── services/ │   │   │   ├── auth.service.ts │   │   │   ├── member.service.ts │   │   │   ├── event.service.ts │   │   │   ├── waitlist.service.ts │   │   │   ├── invoice.service.ts │   │   │   ├── email.service.ts │   │   │   └── file.service.ts │   │   ├── durable-objects/ │   │   │   ├── chat-room.ts    \# ChatRoomDO │   │   │   └── live-stats.ts   \# EventStatsDO │   │   ├── types/ │   │   │   ├── bindings.ts     \# Cloudflare Env Bindings │   │   │   └── api.ts          \# Request/Response-Types │   │   └── utils/ │   │       ├── jwt.ts          \# Ed25519 JWT-Helfer │   │       ├── pagination.ts   \# Pagination-Helfer │   │       ├── qr-code.ts      \# QR-Code-Generierung │   │       └── errors.ts       \# Custom Error Classes │   ├── wrangler.toml             \# Cloudflare Worker Config │   ├── drizzle.config.ts         \# Drizzle Kit Config │   ├── package.json │   └── tsconfig.json ├── package.json              \# Root (Frontend) └── ... |
| :---- |

| \== WRANGLER.TOML \== Erstelle eine vollständige wrangler.toml für den API-Worker: name \= "verein-connect-api" main \= "src/index.ts" compatibility\_date \= "2024-12-01" compatibility\_flags \= \["nodejs\_compat"\] \# D1 Datenbank \[\[d1\_databases\]\] binding \= "DB" database\_name \= "verein-connect-db" database\_id \= "\<wird nach d1 create eingefügt\>" \# R2 Bucket für Dateien \[\[r2\_buckets\]\] binding \= "FILES" bucket\_name \= "verein-connect-files" \# KV für Sessions/Cache \[\[kv\_namespaces\]\] binding \= "KV" id \= "\<wird nach kv create eingefügt\>" \# Durable Objects \[durable\_objects\] bindings \= \[   { name \= "CHAT\_ROOM", class\_name \= "ChatRoomDO" },   { name \= "EVENT\_STATS", class\_name \= "EventStatsDO" } \] \[\[migrations\]\] tag \= "v1" new\_classes \= \["ChatRoomDO", "EventStatsDO"\] \# CORS für Frontend \[vars\] FRONTEND\_URL \= "https://verein-connect.pages.dev" JWT\_ISSUER \= "verein-connect-api" \# Secrets (via wrangler secret put): \# JWT\_PRIVATE\_KEY  (Ed25519 PEM) \# JWT\_PUBLIC\_KEY   (Ed25519 PEM) \# STRIPE\_SECRET\_KEY \# RESEND\_API\_KEY   (für E-Mail-Versand) |
| :---- |

| \== CLOUDFLARE BINDINGS TYPE \== // api/src/types/bindings.ts export interface Env {   DB: D1Database;   FILES: R2Bucket;   KV: KVNamespace;   CHAT\_ROOM: DurableObjectNamespace;   EVENT\_STATS: DurableObjectNamespace;   FRONTEND\_URL: string;   JWT\_ISSUER: string;   JWT\_PRIVATE\_KEY: string;   JWT\_PUBLIC\_KEY: string;   STRIPE\_SECRET\_KEY: string;   RESEND\_API\_KEY: string; } |
| :---- |

## **Phase 1b: D1 Datenbank-Schema (Drizzle)**

| \== D1 SCHEMA (Drizzle ORM) \== Erstelle das Drizzle-Schema in api/src/db/schema.ts mit ALLEN Tabellen. Verwende die Drizzle SQLite-Adapter-Syntax (drizzle-orm/sqlite-core). KERN-TABELLEN: 1\. organizations    id (text PK, UUID), name, slug (unique), logo\_url, settings (text/JSON),    plan (text: free|basic|pro|enterprise), created\_at, updated\_at 2\. users    id (text PK, UUID), org\_id (FK organizations), email, password\_hash,    first\_name, last\_name, display\_name, avatar\_url, status    (active|inactive|pending|blocked), member\_number, qr\_code (UUID),    last\_login, created\_at, updated\_at    UNIQUE(org\_id, email)    Indizes: org\_id, (org\_id+email), (org\_id+status), qr\_code 3\. profile\_field\_definitions    id, org\_id (FK), category, field\_name, field\_label, field\_type    (text|number|date|select|checkbox|url), options (JSON), is\_required,    is\_searchable, is\_visible\_registration, sort\_order,    gdpr\_retention\_days, created\_at    UNIQUE(org\_id, field\_name) 4\. profile\_field\_values    id, user\_id (FK ON DELETE CASCADE), field\_id (FK ON DELETE CASCADE),    value (text), created\_at, updated\_at    UNIQUE(user\_id, field\_id) 5\. roles    id, org\_id (FK), name, description, category    (general|team|department|system), is\_system, max\_members,    permissions (JSON array), parent\_role\_id (FK self), sort\_order,    created\_at    UNIQUE(org\_id, name) 6\. user\_roles    id, user\_id (FK CASCADE), role\_id (FK CASCADE), is\_leader,    start\_date, end\_date, status (active|pending|expired), created\_at    UNIQUE(user\_id, role\_id, start\_date) 7\. families    id, org\_id (FK), name, primary\_contact\_id (FK users),    discount\_percent (real), created\_at 8\. family\_members    id, family\_id (FK CASCADE), user\_id (FK CASCADE),    relationship (parent|child|spouse|member), is\_billing\_contact    UNIQUE(family\_id, user\_id) 9\. event\_categories    id, org\_id (FK), name, color, icon, sort\_order    UNIQUE(org\_id, name) 10\. events     id, org\_id (FK), category\_id (FK), title, description,     event\_type (single|recurring|course), location, start\_date, end\_date,     recurrence\_rule (iCal RRULE), max\_participants, registration\_deadline,     cancellation\_deadline, fee\_amount (real), fee\_currency, auto\_invoice,     is\_public, status (draft|active|cancelled|completed),     created\_by (FK users), created\_at, updated\_at 11\. event\_occurrences     id, event\_id (FK CASCADE), start\_date, end\_date, is\_cancelled,     override\_location, notes 12\. event\_target\_roles     event\_id (FK CASCADE), role\_id (FK CASCADE), max\_from\_role     PK(event\_id, role\_id) 13\. event\_registrations     id, event\_id (FK CASCADE), occurrence\_id (FK, nullable),     user\_id (FK), status (registered|waitlist|cancelled|attended),     waitlist\_position, cancellation\_reason, registered\_at,     registered\_by (FK users), invoice\_id (FK invoices, nullable)     UNIQUE(event\_id, user\_id, occurrence\_id) 14\. event\_leaders     event\_id (FK CASCADE), user\_id (FK), role\_label, show\_on\_registration     PK(event\_id, user\_id) 15\. attendance     id, occurrence\_id (FK CASCADE), user\_id (FK),     status (present|absent|excused|late), checked\_in\_at,     checked\_in\_by (FK users), check\_in\_method (manual|qr\_scan|auto),     notes     UNIQUE(occurrence\_id, user\_id) 16\. messages     id, org\_id (FK), sender\_id (FK users), channel     (email|push|chat|sms|homepage), subject, body, template\_id (FK),     is\_published, publish\_date, status (draft|sent|scheduled|failed),     scheduled\_at, sent\_at, created\_at 17\. message\_recipients     id, message\_id (FK CASCADE), recipient\_type (role|user|all),     recipient\_id, delivery\_status (pending|delivered|read|bounced),     delivered\_at 18\. message\_templates     id, org\_id (FK), name, channel, subject, body, signature, created\_at 19\. invoices     id, org\_id (FK), user\_id (FK), invoice\_number (unique per org),     type (invoice|credit\_note|reminder),     status (draft|sent|paid|overdue|cancelled),     subtotal, tax\_rate, tax\_amount, total (real), currency, due\_date,     paid\_at, payment\_method, stripe\_payment\_id, pdf\_url, notes, created\_at 20\. invoice\_items     id, invoice\_id (FK CASCADE), description, quantity (real),     unit\_price (real), total (real), event\_id (FK, nullable), sort\_order 21\. accounting\_entries     id, org\_id (FK), invoice\_id (FK, nullable), entry\_date,     type (income|expense), category, description, amount (real),     payment\_method, receipt\_url, created\_by (FK users), created\_at 22\. shop\_products     id, org\_id (FK), category, name, description, price (real),     currency, stock, image\_url, is\_active, members\_only, created\_at 23\. shop\_orders     id, org\_id (FK), user\_id (FK), status     (pending|paid|shipped|completed|cancelled), total (real),     invoice\_id (FK, nullable), created\_at 24\. files     id, org\_id (FK), folder\_path, file\_name, mime\_type, size\_bytes,     r2\_key, uploaded\_by (FK users), access\_roles (JSON), created\_at 25\. saved\_filters     id, org\_id (FK), created\_by (FK users), name, entity\_type     (users|events|invoices), filter\_rules (JSON), columns (JSON),     is\_favorite, is\_shared, created\_at 26\. audit\_log     id, org\_id (FK), user\_id (FK), action (text), entity\_type (text),     entity\_id (text), details (JSON), ip\_address, created\_at Generiere die erste D1-Migration als SQL-Datei mit allen CREATE TABLE und CREATE INDEX Statements. Erstelle auch einen Seed-Script der System-Rollen anlegt (org\_admin, member\_admin, event\_admin, finance\_admin, trainer, member). |
| :---- |

## **Phase 2: Authentifizierung & Autorisierung**

| \== AUTH-SYSTEM \== Implementiere das komplette Auth-System mit folgenden Endpunkten: POST /auth/register   Body: { org\_name, org\_type, first\_name, last\_name, email, password }   Logik:     1\. Validierung mit Zod (E-Mail-Format, Passwort min 8 Zeichen)     2\. Organization erstellen (slug aus org\_name generieren)     3\. User erstellen (password\_hash mit bcryptjs, Runde 12\)     4\. System-Rollen für die neue Org erstellen     5\. User die org\_admin-Rolle zuweisen     6\. JWT-Paar generieren   Response: { user, organization, access\_token, refresh\_token, expires\_in } POST /auth/login   Body: { email, password, org\_slug? }   Logik:     1\. User anhand email \+ org finden     2\. Passwort mit bcryptjs verifizieren     3\. Bei Erfolg: JWT-Paar generieren, last\_login updaten     4\. Bei Fehler: Zähler für fehlgeschlagene Versuche erhöhen   Response: { user, access\_token, refresh\_token, expires\_in } POST /auth/refresh   Body: { refresh\_token }   Logik:     1\. Refresh-Token in KV nachschlagen     2\. Altes Token aus KV löschen (Token-Rotation\!)     3\. Neues JWT-Paar generieren     4\. Neues Refresh-Token in KV speichern (TTL 7 Tage)   Response: { access\_token, refresh\_token, expires\_in } POST /auth/logout   Header: Authorization: Bearer \<access\_token\>   Logik: Refresh-Token aus KV löschen   Response: { success: true } POST /auth/forgot-password   Body: { email }   Logik: Reset-Token generieren, in KV speichern (TTL 1h),          E-Mail via Resend API senden   Response: { message: "Falls die E-Mail existiert..." } POST /auth/reset-password   Body: { token, new\_password }   Logik: Token aus KV validieren, Passwort updaten, Token löschen GET /auth/me   Header: Authorization: Bearer \<access\_token\>   Response: { user (inkl. roles\[\], permissions\[\], org) } \== JWT-IMPLEMENTIERUNG \== Verwende die jose Library für Ed25519 JWT: Access Token Payload:   { sub: user\_id, org: org\_id, roles: \['trainer','member'\],     permissions: \['members.read','attendance.write',...\],     iss: 'verein-connect-api', iat, exp (15min) } Refresh Token:   Einfache UUID, gespeichert in KV mit Key "refresh:\<uuid\>"   Value: JSON { user\_id, org\_id, created\_at }   TTL: 604800 (7 Tage) \== AUTH-MIDDLEWARE \== Erstelle Hono-Middleware für api/src/middleware/auth.ts:   1\. Authorization Header extrahieren   2\. JWT mit öffentlichem Ed25519-Key verifizieren (jose)   3\. Payload in c.set('user', ...) speichern   4\. 401 bei fehlendem/ungültigem Token Erstelle RBAC-Middleware für api/src/middleware/rbac.ts:   requirePermission('members.write') → prüft gegen JWT permissions\[\]   requireRole('org\_admin') → prüft gegen JWT roles\[\] Erstelle Org-Context-Middleware für api/src/middleware/org-context.ts:   Extrahiert org\_id aus JWT und injiziert in alle DB-Queries   (verhindert Cross-Tenant-Zugriff) |
| :---- |

## **Phase 3: Mitglieder, Rollen & Profilfelder APIs**

| \== MITGLIEDER-API (/v1/members) \== Alle Endpunkte benötigen Auth-Middleware \+ Org-Context. GET /v1/members   Query-Params: search, status, role\_id, page, per\_page (default 25),                 sort\_by (name|email|created\_at), sort\_order (asc|desc)   Response: { data: Member\[\], meta: { total, page, per\_page, total\_pages } }   Member inkludiert: user-Felder \+ roles\[\] \+ profile\_fields{}   Logik: LEFT JOIN user\_roles \+ roles, Subquery für profile\_fields   WICHTIG: Immer WHERE org\_id \= \<aus JWT\> filtern\! GET /v1/members/:id   Response: Vollständiges Member-Objekt mit:     \- Basis-Daten (users-Tabelle)     \- roles\[\] (aus user\_roles JOIN roles)     \- profile\_fields {} (aus profile\_field\_values JOIN definitions)     \- family (aus family\_members JOIN families, falls vorhanden)     \- stats { attendance\_rate, last\_attendance, events\_count } POST /v1/members   Body: { first\_name, last\_name, email, status?, role\_ids?,           profile\_fields?: { field\_id: value }\[\], password? }   Logik:     1\. Zod-Validierung     2\. User in users-Tabelle erstellen     3\. QR-Code UUID generieren     4\. Rollen zuweisen (user\_roles)     5\. Profilfelder speichern (profile\_field\_values)     6\. Audit-Log schreiben   Permissions: members.write PATCH /v1/members/:id   Body: Partial\<Member\> (nur geänderte Felder)   Logik: Felder updaten, Profilfelder upserten, Audit-Log   Permissions: members.write ODER eigenes Profil (profile.own) DELETE /v1/members/:id   Logik: Soft-Delete (status \= 'blocked') oder Hard-Delete (DSGVO)   Permissions: members.delete GET /v1/members/:id/qr-code   Response: QR-Code als SVG oder PNG (generiert aus qr\_code UUID) POST /v1/members/import   Body: FormData mit CSV/Excel-Datei   Logik: CSV parsen, validieren, Batch-Insert   Response: { imported: N, errors: \[{row, message}\] } GET /v1/members/export   Query: format (csv|xlsx|dosb-xml), filter (gleiche wie GET /members)   Response: Datei-Download POST /v1/members/bulk   Body: { action: 'status\_change'|'role\_assign'|'send\_invoice',           member\_ids: string\[\], params: {...} }   Logik: Massenaktion ausführen, Ergebnis zurückgeben \== ROLLEN-API (/v1/roles) \== GET    /v1/roles                → Alle Rollen der Org POST   /v1/roles                → Neue Rolle erstellen PATCH  /v1/roles/:id            → Rolle bearbeiten DELETE /v1/roles/:id            → Rolle löschen (nicht System-Rollen\!) GET    /v1/roles/:id/members    → Mitglieder einer Rolle POST   /v1/roles/:id/members    → User einer Rolle zuweisen DELETE /v1/roles/:id/members/:uid → User aus Rolle entfernen \== PROFILFELD-API (/v1/settings/fields) \== GET    /v1/settings/fields      → Alle Felddefinitionen der Org POST   /v1/settings/fields      → Neues Feld erstellen PATCH  /v1/settings/fields/:id  → Feld bearbeiten DELETE /v1/settings/fields/:id  → Feld löschen (+ alle Werte CASCADE) PUT    /v1/settings/fields/order → Sortierreihenfolge updaten Permissions: settings.write \== FAMILIEN-API (/v1/families) \== GET    /v1/families              → Alle Familien der Org POST   /v1/families              → Familie erstellen PATCH  /v1/families/:id          → Familie bearbeiten DELETE /v1/families/:id          → Familie löschen POST   /v1/families/:id/members  → Mitglied zur Familie hinzufügen DELETE /v1/families/:fid/members/:uid → Mitglied entfernen \== GESPEICHERTE FILTER (/v1/filters) \== GET    /v1/filters               → Alle Filter des Users POST   /v1/filters               → Filter speichern PATCH  /v1/filters/:id           → Filter bearbeiten DELETE /v1/filters/:id           → Filter löschen POST   /v1/filters/:id/execute   → Filter ausführen, Ergebnisse liefern |
| :---- |

## **Phase 4: Events, Kurse & Anwesenheit**

| \== EVENTS-API (/v1/events) \== GET /v1/events   Query: search, category\_id, event\_type, status, start\_after,          start\_before, page, per\_page, sort\_by, sort\_order   Response: { data: Event\[\], meta: PaginationMeta }   Event inkludiert: category, leaders\[\], registration\_count,     waitlist\_count, user\_registration\_status (für eingeloggten User) GET /v1/events/:id   Response: Vollständiges Event mit occurrences\[\], leaders\[\],     target\_roles\[\], registration\_stats POST /v1/events   Body: { title, category\_id, event\_type, location, start\_date,           end\_date, recurrence\_rule?, max\_participants?,           fee\_amount?, auto\_invoice?, is\_public?,           leader\_ids?, target\_role\_ids?, ... }   Logik:     1\. Event erstellen     2\. Bei recurring: Occurrences aus RRULE generieren        (Verwende eine RRULE-Parsing-Logik, z.B. manuell oder        einfache Implementierung für WEEKLY/DAILY)     3\. Target-Roles zuweisen     4\. Leaders zuweisen   Permissions: events.write PATCH /v1/events/:id DELETE /v1/events/:id \== ANMELDUNGS-API (/v1/events/:id/registrations) \== POST /v1/events/:id/register   Body: { user\_id?, occurrence\_id? }   (user\_id optional – wenn leer, wird eingeloggter User genommen)   Logik:     1\. Prüfen: Ist User bereits angemeldet?     2\. Prüfen: Max-Teilnehmer erreicht?     3\. Prüfen: Rollenbasierte Limits (event\_target\_roles.max\_from\_role)     4\. Prüfen: Registration-Deadline nicht überschritten?     5\. Wenn Platz frei: status \= 'registered'        Wenn voll: status \= 'waitlist', waitlist\_position berechnen     6\. Wenn auto\_invoice \= true: Rechnung erstellen (invoice.service)     7\. Benachrichtigung senden (optional)   Response: { registration, invoice? } DELETE /v1/events/:id/register   Body: { user\_id?, cancellation\_reason? }   Logik:     1\. Registration auf 'cancelled' setzen     2\. WARTELISTEN-NACHRÜCKEN:        a. Nächsten User auf Warteliste finden (niedrigste Position)        b. Status auf 'registered' setzen        c. Benachrichtigung senden: "Du bist nachgerückt\!"     3\. Wenn auto\_invoice: Rechnung stornieren oder Gutschrift   WICHTIG: Atomare Operation\! Verwende D1 Transaction oder            Durable Object für Konsistenz. GET /v1/events/:id/registrations   Response: { registered: User\[\], waitlist: User\[\], cancelled: User\[\] } \== KURS-SPEZIFISCHE ENDPUNKTE \== GET  /v1/courses → Alias für GET /v1/events?event\_type=course POST /v1/courses → Alias für POST /v1/events mit event\_type='course' (Kurse sind Events mit event\_type='course' \+ zusätzlichen Feldern) \== ANWESENHEITS-API (/v1/attendance) \== GET /v1/attendance/today   Response: Alle heutigen Occurrences mit Teilnehmerliste \+ Status GET /v1/attendance/:occurrence\_id   Response: Liste aller registrierten Teilnehmer mit Anwesenheitsstatus POST /v1/attendance/:occurrence\_id/check-in   Body: { user\_id, status: 'present'|'absent'|'excused'|'late',           method: 'manual'|'qr\_scan' }   Logik:     1\. Attendance-Eintrag erstellen/updaten (UPSERT)     2\. Live-Stats via Durable Object updaten (EventStatsDO)   Permissions: attendance.write POST /v1/attendance/:occurrence\_id/check-in/qr   Body: { qr\_code }  (die UUID vom User QR-Code)   Logik: User anhand qr\_code finden, Check-In ausführen POST /v1/attendance/:occurrence\_id/bulk   Body: { entries: \[{ user\_id, status }\] }   Logik: Batch-Update aller Einträge GET /v1/attendance/stats   Query: user\_id?, event\_id?, date\_from?, date\_to?   Response: { attendance\_rate, total\_sessions, present, absent,              excused, by\_month: \[{month, rate}\] } \== DURABLE OBJECT: EventStatsDO \== Echtzeit-Teilnahmestatistiken via WebSocket:   \- Endpoint: /v1/events/:id/live-stats (WebSocket-Upgrade)   \- Bei jedem Check-In: Broadcast an alle verbundenen Clients   \- Payload: { total\_registered, checked\_in, absent, pending }   \- Frontend verbindet sich beim Öffnen des Check-In-Screens |
| :---- |

## **Phase 5: Kommunikation & Chat**

| \== NACHRICHTEN-API (/v1/messages) \== GET    /v1/messages              → Alle Nachrichten (Sent/Draft/Scheduled) POST   /v1/messages              → Nachricht erstellen GET    /v1/messages/:id          → Nachricht mit Empfängern \+ Status PATCH  /v1/messages/:id          → Entwurf bearbeiten DELETE /v1/messages/:id          → Nachricht löschen POST   /v1/messages/:id/send     → Nachricht jetzt senden Sende-Logik (POST /v1/messages/:id/send):   1\. Empfänger auflösen (Rolle → User-Liste, 'all' → alle aktiven User)   2\. Template-Tags ersetzen: {{vorname}}, {{nachname}}, {{verein}} etc.   3\. Pro Kanal senden:      \- email: Resend API (https://api.resend.com/emails)      \- push: Web Push API (später)      \- homepage: is\_published \= true setzen   4\. message\_recipients Delivery-Status updaten GET /v1/messages/templates       → Alle Vorlagen POST /v1/messages/templates      → Vorlage erstellen PATCH /v1/messages/templates/:id → Vorlage bearbeiten DELETE /v1/messages/templates/:id → Vorlage löschen GET /v1/notifications            → In-App Benachrichtigungen für User   (Gefiltert nach Rollen des Users, sortiert nach Datum) \== CHAT via DURABLE OBJECTS (/v1/chat) \== GET /v1/chat/conversations   Response: Liste der Chat-Konversationen des Users   (Direkt \+ Gruppen), jeweils mit letzter Nachricht \+ Unread-Count GET /v1/chat/conversations/:id/messages   Query: before (cursor-based pagination), limit (default 50\)   Response: { messages: ChatMessage\[\], has\_more: boolean } WebSocket: /v1/chat/ws/:conversation\_id   Upgrade zu WebSocket, geroutet an ChatRoomDO   Incoming: { type: 'message', content: string, attachments? }   Outgoing: { type: 'message', from: User, content, timestamp }             { type: 'typing', from: User }             { type: 'read', message\_id } \== DURABLE OBJECT: ChatRoomDO \== class ChatRoomDO {   // WebSocket-Connections pro Conversation   // Storage: Nachrichten persistent im DO-Storage   // Broadcast: Neue Nachrichten an alle verbundenen Clients   // Methoden: handleMessage, handleTyping, getHistory } |
| :---- |

## **Phase 6: Finanzen, Rechnungen & Stripe**

| \== RECHNUNGS-API (/v1/invoices) \== GET /v1/invoices   Query: search (Rechnungsnr/Name), status, date\_from, date\_to,          page, per\_page   Response: { data: Invoice\[\], meta: PaginationMeta,              summary: { total\_open, total\_paid, total\_overdue } } GET    /v1/invoices/:id POST   /v1/invoices            → Rechnung manuell erstellen PATCH  /v1/invoices/:id        → Rechnung bearbeiten (nur Entwurf) DELETE /v1/invoices/:id        → Stornieren POST   /v1/invoices/:id/send   → Per E-Mail senden \+ Status updaten POST   /v1/invoices/:id/paid   → Als bezahlt markieren GET    /v1/invoices/:id/pdf    → PDF generieren/herunterladen POST   /v1/invoices/bulk-remind → Mahnung an alle überfälligen PDF-Generierung (GET /v1/invoices/:id/pdf):   \- HTML-Template mit Vereinslogo, Absender, Empfänger, Positionen   \- Konvertierung zu PDF (z.B. via @pdfme/generator oder     einfaches HTML mit Puppeteer-ähnlichem Ansatz)   \- PDF in R2 speichern, URL in invoices.pdf\_url Automatische Rechnungsnummern:   Format: RE-{YYYY}-{NNNNN} (z.B. RE-2026-00042)   Logik: MAX(invoice\_number) \+ 1 pro Org pro Jahr \== STRIPE-INTEGRATION \== POST /v1/payments/checkout   Body: { invoice\_id }   Logik: Stripe Checkout Session erstellen   Response: { checkout\_url } POST /v1/payments/webhook  (Stripe Webhook, KEIN Auth\!)   Events: checkout.session.completed, payment\_intent.succeeded   Logik: Invoice als 'paid' markieren, stripe\_payment\_id speichern   WICHTIG: Stripe Signature verifizieren\! \== BUCHHALTUNGS-API (/v1/accounting) \== GET /v1/accounting   Query: date\_from, date\_to, type (income|expense), category   Response: { entries: Entry\[\], summary: { income, expense, balance } } POST   /v1/accounting           → Eintrag erstellen PATCH  /v1/accounting/:id       → Eintrag bearbeiten DELETE /v1/accounting/:id       → Eintrag löschen GET    /v1/accounting/export    → Excel-Download GET    /v1/accounting/report   Query: year, granularity (month|quarter)   Response: { periods: \[{ label, income, expense, balance }\] } |
| :---- |

## **Phase 7: Webshop, Dateien, DSGVO & Settings**

| \== WEBSHOP-API (/v1/shop) \== GET    /v1/shop/products         → Alle Produkte POST   /v1/shop/products         → Produkt erstellen (mit Bild-Upload zu R2) PATCH  /v1/shop/products/:id DELETE /v1/shop/products/:id POST   /v1/shop/orders           → Bestellung aufgeben GET    /v1/shop/orders            → Bestellungen (Admin: alle, User: eigene) PATCH  /v1/shop/orders/:id        → Status ändern \== DATEIEN-API (/v1/files) – R2-backed \== GET /v1/files   Query: folder\_path, search   Response: { folders: string\[\], files: File\[\] } POST /v1/files/upload   Body: FormData (file \+ folder\_path \+ access\_roles\[\])   Logik:     1\. Datei in R2 speichern (Key: org\_id/folder\_path/filename)     2\. Metadaten in files-Tabelle   Max File Size: 100MB (R2-Limit prüfen) GET /v1/files/:id/download   Logik: Rollen-Check (access\_roles), dann R2-Presigned URL oder Stream POST   /v1/files/folder          → Ordner erstellen (virtuell in DB) DELETE /v1/files/:id              → Datei aus R2 \+ DB löschen PATCH  /v1/files/:id              → Umbenennen, Zugriffsrechte ändern \== EINSTELLUNGEN-API (/v1/settings) \== GET  /v1/settings/organization    → Org-Einstellungen PATCH /v1/settings/organization   → Org-Einstellungen updaten POST /v1/settings/organization/logo → Logo zu R2 hochladen \== DSGVO & AUDIT \== GET /v1/gdpr/export/:user\_id   Logik: Alle Daten eines Users sammeln (users, profile\_fields,   roles, attendance, invoices, messages) → JSON-Datei   Response: ZIP-Download POST /v1/gdpr/delete/:user\_id   Logik: Alle personenbezogenen Daten löschen, anonymisieren   (Name → "Gelöschter User", E-Mail → NULL, etc.) GET /v1/audit-log   Query: user\_id?, action?, entity\_type?, date\_from?, date\_to?,          page, per\_page   Response: Paginierte Audit-Log-Einträge   Permissions: settings.read (nur Admins) Audit-Log wird automatisch geschrieben bei:   \- Jeder CREATE/UPDATE/DELETE-Operation auf sensiblen Entitäten   \- Login/Logout-Events   \- Rollenänderungen   \- DSGVO-Aktionen \== CRON / SCHEDULED WORKERS \== Registriere Cron-Trigger in wrangler.toml: \[triggers\] crons \= \[   "0 2 \* \* \*",    \# Täglich 02:00: DSGVO-Datenbereinigung   "0 8 \* \* 1",    \# Montags 08:00: Überfällige Rechnungen markieren   "0 0 1 \* \*"     \# Monatlich: Mitgliedsbeiträge generieren \] Implementiere scheduled()-Handler in index.ts der diese Jobs ausführt. |
| :---- |

## **Wichtige Implementierungs-Patterns**

| \== HONO APP STRUCTURE (api/src/index.ts) \== import { Hono } from 'hono'; import { cors } from 'hono/cors'; import { logger } from 'hono/logger'; import { Env } from './types/bindings'; import { authMiddleware } from './middleware/auth'; import { authRoutes } from './routes/auth'; import { memberRoutes } from './routes/members'; // ... weitere Imports const app \= new Hono\<{ Bindings: Env }\>(); // Globale Middleware app.use('\*', logger()); app.use('\*', cors({   origin: (origin, c) \=\> c.env.FRONTEND\_URL,   credentials: true,   allowHeaders: \['Content-Type', 'Authorization'\],   allowMethods: \['GET', 'POST', 'PATCH', 'PUT', 'DELETE', 'OPTIONS'\], })); // Öffentliche Routen (kein Auth) app.route('/auth', authRoutes); // Geschützte Routen app.use('/v1/\*', authMiddleware); app.route('/v1/members', memberRoutes); app.route('/v1/events', eventRoutes); // ... weitere Routen // Health Check app.get('/health', (c) \=\> c.json({ status: 'ok', version: '1.0.0' })); // 404 Handler app.notFound((c) \=\> c.json({ error: 'Not Found' }, 404)); // Error Handler app.onError((err, c) \=\> {   console.error(err);   return c.json({ error: err.message }, err.status || 500); }); export default app; export { ChatRoomDO } from './durable-objects/chat-room'; export { EventStatsDO } from './durable-objects/live-stats'; \== PAGINATION HELPER \== // Einheitliches Pagination-Format für alle List-Endpunkte: interface PaginatedResponse\<T\> {   data: T\[\];   meta: {     total: number;     page: number;     per\_page: number;     total\_pages: number;   }; } // Pagination aus Query-Params extrahieren: function parsePagination(query: Record\<string, string\>) {   const page \= Math.max(1, parseInt(query.page || '1'));   const per\_page \= Math.min(100, Math.max(1,     parseInt(query.per\_page || '25')));   return { page, per\_page, offset: (page \- 1\) \* per\_page }; } \== ERROR HANDLING \== // Custom Error Classes: class AppError extends Error {   constructor(public status: number, message: string,               public code?: string) {     super(message);   } } class NotFoundError extends AppError {   constructor(entity: string) {     super(404, \`${entity} nicht gefunden\`, 'NOT\_FOUND');   } } class ForbiddenError extends AppError {   constructor() {     super(403, 'Keine Berechtigung', 'FORBIDDEN');   } } class ValidationError extends AppError {   constructor(public errors: Record\<string, string\[\]\>) {     super(422, 'Validierungsfehler', 'VALIDATION\_ERROR');   } } // Einheitliches Error-Response-Format: { error: string, code?: string, details?: Record\<string, string\[\]\> } \== MULTI-TENANCY PATTERN \== // JEDE Query MUSS org\_id filtern\! // Verwende den Org-Context aus dem JWT: // In der Middleware: const orgId \= c.get('user').org; // In Drizzle-Queries: const members \= await db.select()   .from(users)   .where(and(     eq(users.orgId, orgId),  // \<-- IMMER\!     eq(users.status, 'active')   ))   .limit(perPage).offset(offset); \== DEPLOYMENT-BEFEHLE \== \# D1 erstellen: wrangler d1 create verein-connect-db \# Migration generieren: npx drizzle-kit generate \# Migration auf D1 ausführen: wrangler d1 execute verein-connect-db \--remote \--file=migrations/0001.sql \# R2 Bucket erstellen: wrangler r2 bucket create verein-connect-files \# KV Namespace erstellen: wrangler kv namespace create KV \# Secrets setzen: wrangler secret put JWT\_PRIVATE\_KEY wrangler secret put JWT\_PUBLIC\_KEY wrangler secret put STRIPE\_SECRET\_KEY wrangler secret put RESEND\_API\_KEY \# Deployen: cd api && wrangler deploy |
| :---- |

*\--- ENDE PROMPT \---*

**Ergebnis:** Claude wird das komplette Backend Schritt für Schritt implementieren. Nach jeder Phase kannst du den Code reviewen, lokal testen (wrangler dev) und deployen.